---
apiVersion: image.toolkit.fluxcd.io/v1beta2
kind: ImagePolicy
metadata:
  name: kbot
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: kbot
  filterTags:
    pattern: '.*-stage.*'
  policy:
    semver:
      range: '^1.x-0'